Developed a Deep Learning-based Email Spam Detection system using TensorFlow and Keras. The project leverages Natural Language Processing techniques such as tokenization and sequence padding, followed by an LSTM neural network for binary classification of emails into spam and non-spam categories. The model is trained and evaluated on a labeled email dataset and can predict the class of new incoming messages with high accuracy.
